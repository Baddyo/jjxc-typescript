enum Idx {
    FIRST,
    SECOND,
    THIRD,
    FOURTH
}

let i: Idx = Idx.FIRST;
let second = Idx[1];

enum LowerChar {
    A = 'a',
        B = 'b',
        C = 'c'
}
let a: LowerChar = LowerChar.A;
let b = LowerChar.B;